---
apiVersion: v1
kind: Service
metadata:
  name: webapp-color
  namespace: python-webapps
  labels:
    app: webapp-color
spec:
  selector:
    app: webapp-color
  ports:
  - protocol: TCP
    targetPort: 8080
    name: http
    port: 8080
